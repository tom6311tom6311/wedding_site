{
  "name": "wedding-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm --workspace @wedding-site/web run dev --",
    "build": "npm --workspace @wedding-site/web run build",
    "preview": "npm --workspace @wedding-site/web run preview --"
  }
}
